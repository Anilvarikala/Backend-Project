<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title><%=pickle.pickleName%> - Details</title>
    <!-- Bootstrap CSS -->
    <link
      href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/css/bootstrap.min.css"
      rel="stylesheet"
    />
    <!-- Font Awesome -->
    <link
      href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.0.0/css/all.min.css"
      rel="stylesheet"
    />
    <style>
      body {
        background-color: #f8f9fa;
        padding: 3rem 0;
      }
      .product-container {
        background: white;
        border-radius: 20px;
        box-shadow: 0 5px 20px rgba(0, 0, 0, 0.1);
        overflow: hidden;
        margin: 0 auto;
        max-width: 1000px;
      }
      .product-image {
        width: 100%;
        height: 400px;
        object-fit: cover;
      }
      .product-info {
        padding: 2.5rem;
      }
      .product-name {
        font-size: 2.5rem;
        color: #2c3e50;
        margin-bottom: 1.5rem;
        font-weight: 600;
      }
      .product-meta {
        background: #f8f9fa;
        border-radius: 10px;
        padding: 1.5rem;
        margin-bottom: 2rem;
      }
      .meta-item {
        display: flex;
        align-items: center;
        margin-bottom: 1rem;
        font-size: 1.1rem;
      }
      .meta-item i {
        width: 30px;
        color: #28a745;
        margin-right: 10px;
      }
      .price {
        font-size: 2rem;
        color: #28a745;
        font-weight: 600;
        display: flex;
        align-items: center;
        gap: 0.5rem;
      }
      .description {
        font-size: 1.1rem;
        line-height: 1.6;
        color: #666;
        margin-top: 1.5rem;
      }
      .back-btn {
        display: inline-block;
        padding: 0.8rem 1.5rem;
        background-color: #28a745;
        color: white;
        text-decoration: none;
        border-radius: 5px;
        margin-top: 2rem;
        transition: all 0.3s ease;
      }
      .back-btn:hover {
        background-color: #218838;
        color: white;
        transform: translateY(-2px);
      }
    </style>
  </head>
  <body>
    <div class="container">
      <div class="product-container">
        <div class="row g-0">
          <div class="col-md-6">
            <img
              src="<%=pickle.url%>"
              alt="<%=pickle.pickleName%>"
              class="product-image"
            />
          </div>
          <div class="col-md-6">
            <div class="product-info">
              <h1 class="product-name"><%=pickle.pickleName%></h1>

              <div class="product-meta">
                <div class="meta-item">
                  <i class="fas fa-weight-hanging"></i>
                  <span>Weight: <%=pickle.grams%> grams</span>
                </div>
                <div class="meta-item price">
                  <i class="fas fa-rupee-sign"></i>
                  <span><%=pickle.price%></span>
                </div>
              </div>

              <div class="description">
                <h4><i class="fas fa-info-circle"></i> Product Description</h4>
                <p><%=pickle.description%></p>
              </div>

              <a href="/all-pickles" class="back-btn">
                <i class="fas fa-arrow-left"></i> Back to All Pickles
              </a>
            </div>
          </div>
        </div>
      </div>
    </div>

    <!-- Bootstrap JS -->
    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/js/bootstrap.bundle.min.js"></script>
  </body>
</html>
