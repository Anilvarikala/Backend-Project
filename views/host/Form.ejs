<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title><%= editing ? 'Edit' : 'Add' %> Pickle</title>
    <!-- Bootstrap CSS -->
    <link
      href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/css/bootstrap.min.css"
      rel="stylesheet"
    />
    <style>
      body {
        background-color: #f8f9fa;
        padding: 40px 0;
      }
      .form-container {
        background-color: white;
        border-radius: 10px;
        padding: 30px;
        box-shadow: 0 0 20px rgba(0, 0, 0, 0.1);
        max-width: 600px;
        margin: 0 auto;
      }
      .form-title {
        color: #333;
        margin-bottom: 30px;
        text-align: center;
        font-weight: 600;
      }
      .form-floating {
        margin-bottom: 20px;
      }
      .btn-submit {
        background-color: #28a745;
        color: white;
        padding: 12px 30px;
        border: none;
        border-radius: 5px;
        width: 100%;
        font-size: 16px;
        font-weight: 500;
        transition: all 0.3s ease;
      }
      .btn-submit:hover {
        background-color: #218838;
        transform: translateY(-2px);
      }
      .form-control:focus {
        border-color: #28a745;
        box-shadow: 0 0 0 0.2rem rgba(40, 167, 69, 0.25);
      }
    </style>
  </head>
  <body>
    <div class="container">
      <div class="form-container">
       
        <h2 class="form-title"><%= editing? 'Edit' : 'Add' %> New Pickle</h2>
        <form
          action="<%= editing ? '/host/edit-pickle/' + currPickle._id : '/host/add-pickle' %>"
          method="POST"
        >
          <div class="form-floating">
            <input
              type="text"
              class="form-control"
              id="pickleName"
              name="pickleName"
              placeholder="Pickle Name"
              value="<%= currPickle?.pickleName || '' %>"
              required
            />
            <label for="pickleName">Pickle Name</label>
          </div>

          <div class="form-floating">
            <input
              type="text"
              class="form-control"
              id="url"
              name="url"
              placeholder="Image URL"
              value="<%= currPickle?.url || '' %>"
              required
            />
            <label for="url">Image URL</label>
          </div>

          <div class="form-floating">
            <input
              type="number"
              class="form-control"
              id="grams"
              name="grams"
              placeholder="Grams"
              min="0"
              value="<%= currPickle?.grams || '' %>"
              required
            />
            <label for="grams">Weight (in grams)</label>
          </div>

          <div class="form-floating">
            <input
              type="number"
              class="form-control"
              id="price"
              name="price"
              placeholder="Price"
              min="0"
              step="0.01"
              value="<%= currPickle?.price || '' %>"
              required
            />
            <label for="price">Price</label>
          </div>

          <div class="form-floating">
            <input
              type="date"
              class="form-control"
              id="data"
              name="data"
              required
            />
            <label for="data">Manufacturing Date</label>
          </div>

          <div class="form-floating mb-4">
            <textarea
              class="form-control"
              id="description"
              name="description"
              placeholder="Enter description..."
              style="height: 100px"
              required
            ><%= currPickle?.description || '' %></textarea>
            <label for="description">Description</label>
          </div>

          <button type="submit" class="btn-submit"><%= editing ? 'Update' : 'Add' %> Pickle</button>
        </form>
      </div>
    </div>

    <!-- Bootstrap JS -->
    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/js/bootstrap.bundle.min.js"></script>
  </body>
</html>
