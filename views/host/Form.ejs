<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8" />
  <meta name="viewport" content="width=device-width, initial-scale=1.0"/>
  <title><%= editing ? 'Edit' : 'Add' %> Pickle</title>

  <!-- Bootstrap CSS -->
  <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/css/bootstrap.min.css" rel="stylesheet"/>
  <link href="https://cdn.jsdelivr.net/npm/bootstrap-icons/font/bootstrap-icons.css" rel="stylesheet"/>

  <style>
    body {
      background: linear-gradient(to right, #fdfbfb, #ebedee);
      padding: 40px 0;
      font-family: 'Segoe UI', Tahoma, Geneva, Verdana, sans-serif;
    }

    .form-container {
      background-color: #ffffff;
      border-radius: 16px;
      padding: 40px;
      box-shadow: 0 10px 30px rgba(0, 0, 0, 0.1);
      max-width: 650px;
      margin: auto;
    }

    .form-title {
      color: #2c3e50;
      margin-bottom: 35px;
      text-align: center;
      font-weight: 700;
      font-size: 28px;
    }

    .form-floating {
      margin-bottom: 24px;
    }

    .btn-submit {
      background: linear-gradient(to right, #28a745, #218838);
      color: white;
      padding: 14px;
      border: none;
      border-radius: 8px;
      width: 100%;
      font-size: 17px;
      font-weight: 600;
      transition: all 0.3s ease;
    }

    .btn-submit:hover {
      filter: brightness(0.95);
      transform: scale(1.02);
    }

    .form-control:focus {
      border-color: #28a745;
      box-shadow: 0 0 0 0.2rem rgba(40, 167, 69, 0.25);
    }
  </style>
</head>

<body>

  <div class="container">
    <div class="form-container">
      <h2 class="form-title"><%= editing ? 'Edit' : 'Add' %> Pickle</h2>

      <form action="<%= editing ? '/host/edit-pickle/' + currPickle._id : '/host/add-pickle' %>" method="POST">

        <div class="form-floating">
          <input type="text" class="form-control" id="pickleName" name="pickleName" placeholder="Pickle Name" value="<%= currPickle?.pickleName || '' %>" required />
          <label for="pickleName">Pickle Name</label>
        </div>

        <div class="form-floating">
          <input type="text" class="form-control" id="url" name="url" placeholder="Image URL" value="<%= currPickle?.url || '' %>" required />
          <label for="url">Image URL</label>
        </div>

        <div class="form-floating">
          <input type="number" class="form-control" id="grams" name="grams" placeholder="Weight in grams" min="0" value="<%= currPickle?.grams || '' %>" required />
          <label for="grams">Weight (in grams)</label>
        </div>

        <div class="form-floating">
          <input type="number" class="form-control" id="price" name="price" placeholder="Price" min="0" step="0.01" value="<%= currPickle?.price || '' %>" required />
          <label for="price">Price (in ₹)</label>
        </div>

        <div class="form-floating">
          <input type="date" class="form-control" id="data" name="data" value="<%= currPickle?.data ? currPickle.data.toISOString().split('T')[0] : '' %>" required />
          <label for="data">Manufacturing Date</label>
        </div>

        <div class="form-floating mb-4">
          <textarea class="form-control" id="description" name="description" placeholder="Enter description..." style="height: 120px" required><%= currPickle?.description || '' %></textarea>
          <label for="description">Description</label>
        </div>

        <button type="submit" class="btn-submit">
          <i class="bi bi-check-circle-fill me-2"></i>
          <%= editing ? 'Update' : 'Add' %> Pickle
        </button>

      </form>
    </div>
  </div>

  <!-- Bootstrap JS -->
  <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/js/bootstrap.bundle.min.js"></script>
</body>
</html>
